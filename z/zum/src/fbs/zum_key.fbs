namespace Zum.fbs;
table KeyID {
  id:string;
}
table KeyIDList {
  list:[string];
}
table Key {
  user_i_d:uint64;
  id:string (key);	// 16 byte API key ID, base64 encoded
  secret:[ubyte];	// 32 byte HMAC-SHA256 API secret
}
table KeyUpdAck {
  key:Key;
  ok:uint8;
}
table KeyList {
  list:[Key];
}
